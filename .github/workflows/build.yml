name: Build Project
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Build
        run: make build
      - name: Run
        run: make run
    # FIXME
    # - name: Test
    #   run: make test
